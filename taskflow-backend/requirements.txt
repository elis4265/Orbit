# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.136.3
uvicorn==0.49.0
starlette==1.3.1
python-multipart==0.0.32        # required by FastAPI for form-data (OAuth2PasswordRequestForm)
httptools==0.8.0
watchfiles==1.2.0
anyio==4.13.0
sniffio==1.3.1
h11==0.16.0
asgiref==3.11.1
websockets==16.0                # WebSocket support (real-time board updates)

# ── ORM & migrations ──────────────────────────────────────────────────────────
# Design decision: SQLAlchemy 2.0 async — unified ORM with async sessions;
# avoids running a sync connection pool alongside an async event loop.
SQLAlchemy==2.0.50
asyncpg==0.31.0                 # async PostgreSQL driver
greenlet==3.5.1
alembic==1.18.4
Jinja2==3.1.6                   # alembic migration templates
Mako==1.3.12
MarkupSafe==3.0.3

# ── Auth & security ───────────────────────────────────────────────────────────
# Design decision: PyJWT (not python-jose) + argon2-cffi (not passlib/bcrypt).
# python-jose has known CVEs; passlib is unmaintained. argon2id is the OWASP
# recommended algorithm for password hashing as of 2024.
PyJWT==2.13.0
argon2-cffi==25.1.0
argon2-cffi-bindings==25.1.0
cffi==2.0.0
pycparser==3.0
cryptography==48.0.1

# ── Rate limiting ─────────────────────────────────────────────────────────────
# Design decision: slowapi wraps limits; single decorator on route functions.
# Register: 5/hour (prevents account enumeration bursts).
# Login: 10/minute (allows legitimate retries, blocks credential stuffing).
# verify-email: 5/hour (6-digit code brute-force window: 10^6 / 5 = 200k hours).
slowapi==0.1.9
limits==5.8.0
deprecated==1.3.1
wrapt==2.2.1

# ── Validation ────────────────────────────────────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.1
pydantic_core==2.46.4
annotated-types==0.7.0
annotated-doc==0.0.4
typing-inspection==0.4.2
typing_extensions==4.15.0
email-validator==2.3.0          # required by Pydantic EmailStr
idna==3.18
dnspython==2.8.0

# ── HTTP client ───────────────────────────────────────────────────────────────
# httpx — async client used in tests (ASGITransport) and healthchecks
httpx==0.28.1
httpcore==1.0.9
certifi==2026.5.20
charset-normalizer==3.4.7
urllib3==2.7.0

# ── Task scheduler ───────────────────────────────────────────────────────────
apscheduler==3.10.4

# ── Redis / caching ───────────────────────────────────────────────────────────
# Design decision: Redis for token blacklist and rate-limit state (not in-memory)
# so state survives worker restarts in multi-worker deployments.
redis==8.0.0

# ── Object storage (MinIO) ────────────────────────────────────────────────────
# Design decision: minio Python SDK (synchronous) wrapped in asyncio.to_thread.
# Chosen over boto3 for simplicity; S3-compatible so switching to AWS S3 later
# only requires changing endpoint + credentials.
minio==7.2.15

# ── Email (Brevo) ────────────────────────────────────────────────────────────
# Design decision: Brevo over Resend — free tier sends to any recipient without
# domain verification; only sender email address needs to be verified.
# Uses httpx (already a dependency) via Brevo REST API v3.

# ── AI (OpenAI) ───────────────────────────────────────────────────────────────
openai==2.41.0
jiter==0.15.0
tqdm==4.68.2
distro==1.9.0

# ── Observability ─────────────────────────────────────────────────────────────
# OTel SDK — no-op unless OTLP_ENDPOINT env var set; zero overhead in dev.
structlog==26.1.0
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1
opentelemetry-exporter-otlp-proto-http==1.42.1
opentelemetry-exporter-otlp-proto-common==1.42.1
opentelemetry-proto==1.42.1
opentelemetry-instrumentation==0.63b1
opentelemetry-instrumentation-asgi==0.63b1
opentelemetry-instrumentation-fastapi==0.63b1
opentelemetry-instrumentation-sqlalchemy==0.63b1
opentelemetry-semantic-conventions==0.63b1
opentelemetry-util-http==0.63b1
googleapis-common-protos==1.75.0
protobuf==6.33.6
Pygments==2.20.0                # structlog ConsoleRenderer syntax colouring

# ── Full-text search ──────────────────────────────────────────────────────────
# PostgreSQL FTS (tsvector generated column) + pg_trgm — no extra dependency.

# ── Config & CLI utilities ────────────────────────────────────────────────────
python-dotenv==1.2.2
PyYAML==6.0.3
packaging==26.2
click==8.4.1
colorama==0.4.6
iniconfig==2.3.0

# ── Testing ───────────────────────────────────────────────────────────────────
# Design decision: Testcontainers as default — each test function gets an
# ephemeral postgres container so integration tests never pollute the dev DB.
# Set USE_TESTCONTAINERS=0 to fall back to DATABASE_URL from .env.
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-html==4.2.0
pytest-metadata==3.1.1
coverage==7.14.1
respx==0.23.1                   # mock httpx requests in unit tests
testcontainers==4.14.2
docker==7.1.0                   # required by testcontainers
pluggy==1.6.0
